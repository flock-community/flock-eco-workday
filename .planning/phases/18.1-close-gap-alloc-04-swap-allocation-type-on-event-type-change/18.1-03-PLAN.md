---
phase: 18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change
plan: 03
type: execute
wave: 2
depends_on:
  - 18.1-01
files_modified:
  - tests/steps/eventSteps.ts
  - tests/event-workflow.spec.ts
autonomous: true
requirements:
  - ALLOC-04
must_haves:
  truths:
    - "Running the new Playwright test in the full `event-workflow.spec.ts` suite passes against the Plan 01 fix."
    - "The test changes the event type of 'PW Test Hack Day' (which exists as a HACK_DAY-typed event from EVNT-01) to a CONFERENCE-typed value via the UI, saves, and asserts the budget allocation list shows Study Time (not Hack Time) for the participant."
    - "A new reusable step `When_I_change_event_type_to` is exported from `tests/steps/eventSteps.ts`."
    - "The new test depends on the same `PW Test Hack Day` event already created by EVNT-01 (per STATE.md note: this test must be runnable in the full suite, not standalone)."
    - "D-05: Both the Kotlin test (Plan 02) and the Playwright e2e test (this plan) are required for ALLOC-04 coverage — this plan delivers the UI/e2e layer."
    - "D-07: The Playwright e2e test changes the event type in the UI (MUI Select), saves via the dialog, and verifies the persisted allocation type via the participant's budget tab."
    - "D-08: No frontend (React/TS) source changes are needed — the existing event dialog already reads budgetAllocations[] from the event response and renders the correct type after save."
  artifacts:
    - path: "tests/steps/eventSteps.ts"
      provides: "When_I_change_event_type_to(page, eventTypeOption) step helper"
      contains: "export async function When_I_change_event_type_to"
    - path: "tests/event-workflow.spec.ts"
      provides: "EVNT-07 test in `Event Workflow - Modify Allocations` describe block"
      contains: "EVNT-07"
  key_links:
    - from: "EVNT-07 test"
      to: "When_I_change_event_type_to step"
      via: "import from ./steps/eventSteps"
      pattern: "When_I_change_event_type_to"
    - from: "EVNT-07 test"
      to: "Then_budget_tab_shows_event_allocation"
      via: "post-save assertion that participant's budget list now shows Study Time"
      pattern: "Then_budget_tab_shows_event_allocation\\(.*'Study Time'"
---

<objective>
Add a Playwright end-to-end test (EVNT-07) that verifies ALLOC-04 from the UI: open an existing HACK_DAY-typed event, change its event type to one that maps to STUDY_TIME (e.g., `CONFERENCE`), save, and confirm the participant's budget tab now shows a `Study Time` allocation instead of `Hack Time`. Add a small new step helper `When_I_change_event_type_to` to `tests/steps/eventSteps.ts` so the dropdown interaction is reusable.

Purpose: D-05 and D-07 — a UI-level proof that the backend swap propagates through to what an admin sees in the app. Complements the JVM test from Plan 02. D-08 confirms no React source changes are needed; the dialog already binds to the event response.
Output: One new exported step in `tests/steps/eventSteps.ts` and one new `test('EVNT-07: ...')` inside the existing `Event Workflow - Modify Allocations` describe block in `tests/event-workflow.spec.ts`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-CONTEXT.md
@.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-PATTERNS.md

<interfaces>
<!-- Step helper exports the executor MUST reuse. Extracted from codebase. -->

From tests/steps/eventSteps.ts:
- `Given_I_am_on_events_page(page: Page, userKey: string)` — line 8
- `When_I_open_event_by_description(page: Page, description: string)` — line 105
- `When_I_submit_event_form(page: Page)` — line 94
- `Then_budget_tab_shows_event_allocation(page: Page, allocationTypeText: string, hoursOrAmount: string)` — line 252
- Existing MUI Select pattern inside `When_I_fill_event_form` at lines 61-67 (model for the new helper):
  ```
  const eventTypeControl = page
    .locator('.MuiFormControl-root')
    .filter({ hasText: 'Event type' })
    .first();
  await eventTypeControl.getByRole('combobox').click();
  await page.getByRole('option', { name: options.eventType }).click();
  ```

From tests/steps/budgetSteps.ts:
- `Given_I_am_on_budget_tab_for_person(page: Page, adminUserKey: string, personName: string)` — line 8

From tests/event-workflow.spec.ts:
- `Event Workflow - Modify Allocations` describe block at line 177 (already runs `cleanupTestEvents` via the earlier describe — but only the earlier describe creates `PW Test Hack Day` in EVNT-01, so EVNT-07 must remain in the existing file ordered so EVNT-01 has already run in the same file invocation).
- Existing `test.beforeEach` / `test.afterEach` cookie + storage clearing at lines 178-193 — EVNT-07 inherits these by living inside the same describe block.

From workday-application/src/main/react/utils/mappings.ts:
- `EventTypeMappingToDefaultBudgetType` — `FLOCK_HACK_DAY -> HACK`, `CONFERENCE -> STUDY`. (CONFERENCE is the safe option text to switch into for the swap.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `When_I_change_event_type_to` step helper to eventSteps.ts</name>
  <files>tests/steps/eventSteps.ts</files>
  <read_first>
    - tests/steps/eventSteps.ts (entire file — confirm existing exports, MUI Select pattern at lines 61-67, signature/style of other exported step functions, and where to place the new helper without disturbing other exports)
    - .planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-PATTERNS.md (section "MUI Select interaction in Playwright")
  </read_first>
  <action>
    Per D-07: add a new exported async function `When_I_change_event_type_to(page: Page, eventTypeOption: string): Promise<void>` to `tests/steps/eventSteps.ts`. Place it immediately AFTER the existing `When_I_fill_event_form` function (around lines 70-90 of the current file) so related event-form helpers stay grouped.

    Per D-08: no React/TS source changes — only this test-helper file plus the spec change in Task 2 are touched.

    Implementation behavior (do not include code here — see read_first for the exact MUI pattern to copy):
    - Locate the `.MuiFormControl-root` element whose visible text contains `'Event type'`, take `.first()`.
    - Click its `combobox` role to open the MUI Select dropdown.
    - Click the option whose accessible name equals `eventTypeOption`.
    - No `page.keyboard.press('Escape')` afterwards — selecting an option from a single-select MUI dropdown auto-closes (matches the existing `When_I_fill_event_form` behavior at lines 61-67).

    The helper assumes the event dialog is already open (caller responsibility). DO NOT introduce any navigation, dialog-opening, or save behavior in this helper — keep it single-purpose.

    DO NOT modify or refactor any other existing function in `eventSteps.ts`. Existing exports and their behavior must remain identical (surgical change rule).
  </action>
  <verify>
    <automated>npx playwright test --list tests/event-workflow.spec.ts 2>&amp;1 | head -20 &amp;&amp; grep -c "export async function When_I_change_event_type_to" tests/steps/eventSteps.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function When_I_change_event_type_to" tests/steps/eventSteps.ts` returns exactly 1.
    - `grep -c "When_I_fill_event_form\\|When_I_add_participant\\|When_I_submit_event_form\\|When_I_open_event_by_description\\|Then_budget_tab_shows_event_allocation" tests/steps/eventSteps.ts` returns >= 5 (existing exports preserved).
    - `npx playwright test --list tests/event-workflow.spec.ts` exits 0 (TypeScript compiles, no broken imports).
    - `grep -nE "MuiFormControl-root.*Event type|filter\\(\\{ hasText: 'Event type'" tests/steps/eventSteps.ts` returns >= 2 matches (one in existing `When_I_fill_event_form`, one in the new helper).
    - The new helper signature is exactly `export async function When_I_change_event_type_to(page: Page, eventTypeOption: string)` (verifiable by `grep -n "When_I_change_event_type_to(page: Page, eventTypeOption: string)" tests/steps/eventSteps.ts`).
  </acceptance_criteria>
  <done>
    `When_I_change_event_type_to` is exported from `tests/steps/eventSteps.ts`, follows the same MUI Select pattern as `When_I_fill_event_form`, and the file still compiles for Playwright.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add EVNT-07 test (event type swap end-to-end) to event-workflow.spec.ts</name>
  <files>tests/event-workflow.spec.ts</files>
  <read_first>
    - tests/event-workflow.spec.ts (entire file — confirm describe block boundaries, EVNT-01 setup that creates `PW Test Hack Day`, EVNT-03 structure as the closest analog, beforeEach/afterEach inside `Event Workflow - Modify Allocations` at lines 177-193, and the existing imports near the top of the file)
    - tests/steps/eventSteps.ts (confirm the new `When_I_change_event_type_to` export name from Task 1)
    - tests/steps/budgetSteps.ts (confirm `Given_I_am_on_budget_tab_for_person` signature)
    - .planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-PATTERNS.md (section "tests/event-workflow.spec.ts — add type-swap test to existing file")
  </read_first>
  <action>
    Per D-05 and D-07: add a new `test('EVNT-07: Changing event type swaps allocation type for all participants', ...)` INSIDE the existing `test.describe('Event Workflow - Modify Allocations', ...)` block in `tests/event-workflow.spec.ts`. Place it AFTER the current `EVNT-03` test (so EVNT-03 still has a clean event state to mutate). EVNT-07 will mutate `PW Test Hack Day` last; the suite-level `cleanupTestEvents` in the OTHER describe block already deletes leftovers across runs.

    Per D-08: this is a pure test-only addition — verifying that the existing React event dialog correctly renders the new allocation type from the event response without any frontend source edits.

    Import requirements: add `When_I_change_event_type_to` to the existing `from './steps/eventSteps'` import. Do NOT introduce a new import statement if `eventSteps` is already imported — extend the existing destructured import list. Do NOT touch any other imports.

    Test body (call sequence — do not embed implementation code in this action):
    1. `Given_I_am_on_events_page(page, 'bert')` — admin enters the events list.
    2. `When_I_open_event_by_description(page, 'PW Test Hack Day')` — opens the dialog. At this point the event's type is `FLOCK_HACK_DAY` (HACK_TIME).
    3. `When_I_change_event_type_to(page, 'Conference')` — UI option label for `EventType.CONFERENCE` (verify the exact visible option text in the running app; if the label is different, use the exact label).
    4. `When_I_submit_event_form(page)` — clicks Save and waits for networkidle.
    5. `Given_I_am_on_budget_tab_for_person(page, 'bert', 'Pino')` — navigates to Pino's budget tab.
    6. `Then_budget_tab_shows_event_allocation(page, 'Study Time', '8h')` — asserts the event allocation is now Study Time, 8h. (Pino was the sole participant in EVNT-01 with 1 day x 8h.)
    7. Add a NEGATIVE assertion that the budget list does NOT show a `Hack Time` allocation for this event: use the existing locator pattern from `Then_budget_tab_shows_event_allocation` (read the helper to copy its row-locator approach) and assert `.count()` for `Hack Time` rows tied to the event description is 0. If a clean assertion is not straightforward, instead assert that the only event-linked allocation row visible reads `Study Time`.
    8. After the test body, leave the event in CONFERENCE state — do NOT swap back. The next full run will reset via `cleanupTestEvents` from EVNT-01's describe block.

    DO NOT add a new describe block. DO NOT add a separate `test.beforeAll` for event creation — EVNT-07 must run in the full file (per STATE.md "EVNT-06 must run in full suite" precedent).

    DO NOT modify EVNT-01, EVNT-03, EVNT-04, EVNT-05, EVNT-06, or the fixme'd EVNT-02. Surgical change rule.

    If reading the existing helper `Then_budget_tab_shows_event_allocation` reveals it asserts inside a list scoped to the event description, prefer asserting the new `Study Time` row exists (positive assertion) AND that no `Hack Time` row references this same event description (negative assertion) by chaining a second `page.locator(...).filter({ hasText: 'PW Test Hack Day' }).filter({ hasText: 'Hack Time' })` with `.toHaveCount(0)`.
  </action>
  <verify>
    <automated>npx playwright test tests/event-workflow.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EVNT-07: Changing event type swaps allocation type" tests/event-workflow.spec.ts` returns exactly 1.
    - `grep -n "When_I_change_event_type_to" tests/event-workflow.spec.ts` returns at least 1 match (helper is imported and used).
    - `grep -n "Then_budget_tab_shows_event_allocation(page, 'Study Time'" tests/event-workflow.spec.ts` returns at least 1 match.
    - The EVNT-07 test sits inside `test.describe('Event Workflow - Modify Allocations'`. Verify by reading the file: the line number reported by `grep -n "EVNT-07" tests/event-workflow.spec.ts` is greater than the line number of `test.describe('Event Workflow - Modify Allocations'` and less than the closing `});` of that describe.
    - `npx playwright test tests/event-workflow.spec.ts --reporter=line` exits 0 — full file passes (including EVNT-01..EVNT-06 plus EVNT-07). Running EVNT-07 in isolation is NOT required; per STATE.md precedent, dependency on EVNT-01 is allowed.
    - The new test does NOT introduce new fixtures or top-level imports beyond extending the existing `./steps/eventSteps` import.
    - Nyquist check: with Plan 01 reverted, EVNT-07 must fail (the `Study Time` assertion or the `Hack Time` count==0 assertion fires). Executor documents the failing assertion text in the SUMMARY. If the e2e suite is too slow to re-run end-to-end against the reverted backend, the executor MAY substitute the Plan 02 Nyquist evidence and explicitly note this substitution in the SUMMARY — but a manual `Then_budget_tab_shows_event_allocation` failure under reverted backend is the preferred proof.
  </acceptance_criteria>
  <done>
    EVNT-07 lives inside the `Event Workflow - Modify Allocations` describe in `tests/event-workflow.spec.ts`, uses the new step helper, passes in the full file run, and proves the swap is visible to an admin user from the UI.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser -> running Flock Workday backend | E2e tests cross the real auth + HTTP boundary; tests use the `bert` admin user via the same login flow as the rest of the suite. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-E1 | Tampering | E2e mutates shared dev-data event `PW Test Hack Day` | mitigate | EVNT-07 runs last in `Event Workflow - Modify Allocations`; existing `cleanupTestEvents` (called by the earlier describe's `test.beforeAll`) wipes leftover `PW Test Hack Day` events between full-suite runs. Test does NOT touch any non-test events or other participants. |
| T-18.1-E2 | Information Disclosure | Test screenshots/logs leak data | accept | Same posture as the existing `event-workflow.spec.ts` tests; no new credentials or PII written. |
| T-18.1-E3 | Repudiation | False positive in e2e | mitigate | Nyquist gate: executor runs EVNT-07 against the reverted backend (or relies on Plan 02 Nyquist evidence with explicit annotation) — failure must be demonstrated and recorded in the SUMMARY. |
| T-18.1-E4 | Denial of Service | Long e2e run blocks CI | accept | One additional test in an existing describe; runtime impact bounded by the same dialog/save/list-render workflow already exercised by EVNT-03. |
</threat_model>

<verification>
- `npx playwright test tests/event-workflow.spec.ts --reporter=line` exits 0 with EVNT-07 listed in the output.
- The new test reuses existing steps for navigation and assertions — only `When_I_change_event_type_to` is new.
- Other describe blocks and tests in the file pass unchanged.
- Nyquist proof attached to the SUMMARY: either EVNT-07 failing under reverted backend, or an explicit cross-reference to Plan 02's failing test.
</verification>

<success_criteria>
- ALLOC-04 has UI-level regression coverage that exercises the full stack (UI -> EventController -> EventService.syncBudgetAllocations -> persisted allocations -> budget list re-render).
- New step helper `When_I_change_event_type_to` is reusable for future event-type tests.
- Full `event-workflow.spec.ts` suite remains green.
</success_criteria>

<output>
Create `.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-03-SUMMARY.md` when done, including:
- The new step helper signature and where it was inserted
- The full describe-block + test name for EVNT-07
- The exact `npx playwright test` invocation and its passing summary line
- Nyquist proof reference (direct failure or Plan 02 substitution with rationale)
</output>
</content>
</invoke>
