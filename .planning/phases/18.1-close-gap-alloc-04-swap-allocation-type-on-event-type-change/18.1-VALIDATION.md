---
phase: 18.1
slug: close-gap-alloc-04-swap-allocation-type-on-event-type-change
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-29
---

# Phase 18.1 — Validation Strategy

> Reconstructed from artifacts (PLAN/SUMMARY/UAT). Phase was executed and UAT signed off before VALIDATION.md was written; this document closes the documentation gap.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **JVM framework** | JUnit 5 + Spring Boot Test (`@SpringBootTest`), base class `WorkdayIntegrationTest` with `CleanupDbService` per-test reset |
| **JVM config** | `workday-application/pom.xml`; profile `-Pdevelop`; H2 DB |
| **JVM quick command** | `./mvnw -pl workday-application test -Dtest=EventServiceTest -q` |
| **JVM full suite** | `./mvnw -pl workday-application test -q` |
| **E2E framework** | Playwright (TypeScript) |
| **E2E config** | `playwright.config.ts` at project root |
| **E2E quick command** | `npx playwright test tests/event-workflow.spec.ts --reporter=line` |
| **E2E type check** | `npx playwright test --list tests/event-workflow.spec.ts` |
| **Estimated runtime** | ~30s integration; ~2–4 min e2e (requires backend + frontend up) |

---

## Sampling Rate

- **After every task commit:** Run JVM quick command for backend changes; `npx playwright test --list` for e2e changes
- **After every plan wave:** Run full JVM suite + targeted Playwright spec
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30s (integration) / ~4 min (e2e)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 18.1-01-01 | 01 | 1 | ALLOC-04 | T-18.1-01..05 | Delete-all-then-recreate scoped to current person+event; no new auth/data surface | compile | `./mvnw -pl workday-application compile -q` | ✅ `workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt` | ✅ green |
| 18.1-02-01 | 02 | 2 | ALLOC-04 | T-18.1-T1 | Per-test DB reset; no `@Transactional` masking | integration | `./mvnw -pl workday-application test -Dtest=EventServiceTest -q` | ✅ `workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` (2 @Tests) | ✅ green |
| 18.1-03-01 | 03 | 2 | ALLOC-04 | — | Test-only; no auth surface change | e2e helper | `grep -c "export async function When_I_change_event_type_to" tests/steps/eventSteps.ts` → 1 | ✅ `tests/steps/eventSteps.ts:75` | ✅ green |
| 18.1-03-02 | 03 | 2 | ALLOC-04 | — | Reuses existing admin-auth fixtures | e2e | `npx playwright test tests/event-workflow.spec.ts --reporter=line` | ✅ `tests/event-workflow.spec.ts:254` (EVNT-07) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. `WorkdayIntegrationTest` base + `CleanupDbService` already in place; Playwright `eventSteps.ts` helper module already established.*

---

## Manual-Only Verifications

*All phase behaviors have automated verification.*

Note: Plan 03 SUMMARY records that the full e2e run was deferred at write time (backend not running in the SUMMARY author's environment). The subsequent UAT (`18.1-UAT.md` Test 4, `result: pass`) confirms the EVNT-07 Playwright test executes green against a live backend, closing the deferral.

---

## Nyquist Proof

- **EventServiceTest** — both `typeSwapFromHackToStudyDeletesHackAllocationAndCreatesStudyAllocation` and `typeSwapWhenPersonHadBothTimeAllocationsCollapsesToSingleNewType` fail against pre-fix `EventService.kt` and pass against the fix (recorded in `18.1-02-SUMMARY.md` → "Nyquist Result: PASSED").
- **EVNT-07** — UAT confirms participant Pino's `HackTimeBudgetAllocation id=308` and `StudyTimeBudgetAllocation id=1000` are both deleted and exactly one new `STUDY_TIME id=314` is created after the swap (`18.1-UAT.md` Test 2, `result: pass`).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — existing infra sufficient)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (integration); < 4 min (e2e)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-29 (reconstructed post-UAT)
