---
phase: 18.1
slug: close-gap-alloc-04-swap-allocation-type-on-event-type-change
status: verified
threats_open: 0
asvs_level: 2
created: 2026-05-29
---

# Phase 18.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Scope: ALLOC-04 fix — `EventService.syncBudgetAllocations` replaces in-place
> time-allocation `update` with delete-all-then-recreate; plus JVM integration
> test (EventServiceTest) and Playwright e2e (EVNT-07).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| HTTP -> EventController -> EventService | Authenticated admin requests reach `syncBudgetAllocations` via event create/update endpoints. No new boundary introduced. | Event metadata, person UUIDs, budget figures (non-PII admin data) |
| EventService -> BudgetAllocationService | In-process call, transactional context inherited from the calling endpoint. | Budget allocation IDs already loaded from DB for the same event |
| Test JVM -> Spring context (H2) | Integration tests run in-process against the `test` profile with H2; no external network or filesystem trust boundary is crossed. | Generated test fixtures only |
| Playwright browser -> running Flock Workday backend | E2e tests cross the real auth + HTTP boundary using the `bert` admin user via the same login flow as the rest of the suite. No new boundary introduced. | Test event `PW Test Hack Day`, test person `Pino` |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-18.1-01 | Tampering | `EventService.syncBudgetAllocations` delete loop | mitigate | Filter restricted to `HackTimeBudgetAllocation` / `StudyTimeBudgetAllocation` within `personAllocations` (scoped by `existingByPerson[appPerson.uuid]` and `currentPersonUuids`). Only DB-loaded IDs reach `deleteById`. **Evidence:** EventService.kt:244 (filter), EventService.kt:246 (deleteById loop), EventService.kt:210 (`findAllByEventCode(event.code)` scope), EventService.kt:218-221 (per-person grouping). | closed |
| T-18.1-02 | Information Disclosure | Allocation data in response | accept | Admin-only endpoint, unchanged authorization (`@PreAuthorize('EventAuthority.WRITE')` at EventController.kt:125/133/143). `budgetAllocations[]` already in response surface — fix changes only row type/count, not visibility. | closed (accepted) |
| T-18.1-03 | Denial of Service | Unbounded delete loop | accept | Bounded by per-person time-allocation rows for one event (≤2 per D-03). Same bound as existing money/person cleanup loops (EventService.kt:175, 216, 298). | closed (accepted) |
| T-18.1-04 | Elevation of Privilege | Endpoint authorization | accept | No new endpoint, no new authorization path. Existing `@PreAuthorize('EventAuthority.WRITE')` on EventController.kt:125, 133, 143 still gates every call into `syncBudgetAllocations`. | closed (accepted) |
| T-18.1-05 | Repudiation | Audit of allocation deletes | accept | Deletes go through the same `budgetAllocationService.deleteById` path already used at EventService.kt:175 (person-removal), 216 (person-removal), 298 (money-removal). Audit/logging behavior unchanged. | closed (accepted) |
| T-18.1-T1 | Tampering | Test DB leakage between tests | mitigate | `EventServiceTest` extends `WorkdayIntegrationTest()` (EventServiceTest.kt:23). No `@Transactional` annotation. Base class wires `cleanupDbService.cleanup()` via `@AfterEach` at WorkdayIntegrationTest.kt:34-43. | closed |
| T-18.1-T2 | Information Disclosure | Test logs leak credentials | accept | Tests use generated persons via `CreateHelper`; no real PII or secrets. Standard project policy. | closed (accepted) |
| T-18.1-T3 | Repudiation | False positive in JVM test | mitigate | Nyquist gate executed: both tests failed against pre-fix commit `2412639e` (Test 1: `Expected <0>, actual <1>` on `filterIsInstance<HackTime>.size`; Test 2: `Expected value to be true` on `none { HackTime }`). Recorded in 18.1-02-SUMMARY.md lines 19-30. | closed |
| T-18.1-E1 | Tampering | E2e mutates shared `PW Test Hack Day` event | mitigate | EVNT-07 placed last in `Event Workflow - Modify Allocations` describe (event-workflow.spec.ts:254). `cleanupTestEvents` defined at event-workflow.spec.ts:55 and invoked in the earlier describe's `beforeAll` at line 88, wiping leftover events between full-suite runs. Test touches only `PW Test Hack Day` and participant `Pino`. | closed |
| T-18.1-E2 | Information Disclosure | Test screenshots/logs leak data | accept | Same posture as existing `event-workflow.spec.ts` tests; no new credentials/PII written. | closed (accepted) |
| T-18.1-E3 | Repudiation | False positive in e2e | mitigate | Nyquist substitution per plan-permitted fallback (18.1-03-PLAN.md acceptance criterion): full e2e not executed locally (backend not running); Plan 02 JVM Nyquist proof at integration layer substitutes — same code path (`EventService.syncBudgetAllocations`) verified to fail under pre-fix commit. Substitution and rationale recorded in 18.1-03-SUMMARY.md lines 78-85. TypeScript `--list` compile verified. | closed (with documented Nyquist substitution) |
| T-18.1-E4 | Denial of Service | Long e2e run blocks CI | accept | One additional test in existing describe; runtime bounded by same dialog/save/list-render workflow as EVNT-03. | closed (accepted) |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-18.1-02 | T-18.1-02 | Allocation surface unchanged; admin-only endpoint behind `@PreAuthorize('EventAuthority.WRITE')`. Fix only mutates row type/count, not visibility. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-03 | T-18.1-03 | Per-person time-allocation rows bounded ≤2 per D-03; same bound as pre-existing cleanup loops in the same method. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-04 | T-18.1-04 | No new endpoint and no new authorization path. Existing `@PreAuthorize` at EventController.kt:125/133/143 still gates every entry. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-05 | T-18.1-05 | `budgetAllocationService.deleteById` is the same auditable path used at EventService.kt:175, 216, 298. No new auditing behavior introduced. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-T2 | T-18.1-T2 | Tests use `CreateHelper`-generated persons; no real PII/secrets. Matches existing test policy. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-E2 | T-18.1-E2 | Identical posture to existing `event-workflow.spec.ts`; no new credentials/PII. | gsd-security-auditor | 2026-05-29 |
| AR-18.1-E4 | T-18.1-E4 | One added test in an existing describe; runtime impact comparable to EVNT-03. | gsd-security-auditor | 2026-05-29 |

*Accepted risks do not resurface in future audit runs.*

---

## Unregistered Flags

None. No `## Threat Flags` section was present in 18.1-01-SUMMARY.md, 18.1-02-SUMMARY.md, or 18.1-03-SUMMARY.md. Executors did not report new attack surface beyond the plan-time threat register.

---

## Evidence Index

| Threat | File | Line(s) | Evidence Excerpt |
|--------|------|---------|------------------|
| T-18.1-01 | workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt | 210, 218-221, 235, 244, 246 | `findAllByEventCode(event.code)`; per-person grouping by `currentPersonUuids`; `personAllocations.filter { it is HackTimeBudgetAllocation || it is StudyTimeBudgetAllocation }`; `existingTimeAllocations.forEach { budgetAllocationService.deleteById(it.id) }` |
| T-18.1-02 / T-18.1-04 | workday-application/src/main/kotlin/community/flock/eco/workday/application/controllers/EventController.kt | 125, 133, 143 | `@PreAuthorize("hasAuthority('EventAuthority.WRITE')")` on write endpoints |
| T-18.1-03 | workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt | 243-246 | Filter scoped to `personAllocations` (single person+event) — bounded ≤2 per D-03 |
| T-18.1-05 | workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt | 175, 216, 246, 298 | Same `budgetAllocationService.deleteById` path used throughout (person removal, time removal, money removal) |
| T-18.1-T1 | workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt; .../WorkdayIntegrationTest.kt | 23 (extends); 34, 43 (cleanup) | `class EventServiceTest(...) : WorkdayIntegrationTest()`; base class `@AfterEach cleanupDbService.cleanup()` |
| T-18.1-T3 | .planning/phases/18.1-.../18.1-02-SUMMARY.md | 19-30 | Pre-fix run: `Tests run: 2, Failures: 2` with concrete assertion messages |
| T-18.1-E1 | tests/event-workflow.spec.ts | 55, 88, 254 | `cleanupTestEvents` defined; called in earlier describe `beforeAll`; EVNT-07 last in Modify Allocations describe |
| T-18.1-E3 | .planning/phases/18.1-.../18.1-03-SUMMARY.md | 78-85 | Plan 02 Nyquist substitution explicitly recorded (plan-permitted fallback) |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-29 | 12 | 12 | 0 | gsd-security-auditor |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-29
