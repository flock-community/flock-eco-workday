---
phase: 18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change
plan: 02
type: execute
wave: 2
depends_on:
  - 18.1-01
files_modified:
  - workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt
autonomous: true
requirements:
  - ALLOC-04
must_haves:
  truths:
    - "Running the new integration test in isolation passes against the Plan 01 fix."
    - "The test fails (red) if the Plan 01 fix is reverted, proving Nyquist validity."
    - "The test exercises the FLOCK_HACK_DAY -> CONFERENCE swap (HACK_TIME -> STUDY_TIME) using `eventService.create` followed by `eventService.update`."
    - "The test asserts ZERO HackTimeBudgetAllocation rows remain after the swap, and exactly ONE StudyTimeBudgetAllocation row remains per participant."
    - "A second test case (or assertion within the swap test) confirms that when a participant had BOTH a HackTimeBudgetAllocation AND a StudyTimeBudgetAllocation before the swap, both are deleted and exactly one of the new type remains (covers D-03)."
    - "D-05: Both a Kotlin unit/integration test (this plan) AND a Playwright e2e test (Plan 03) are required — this plan delivers the JVM-level test layer."
    - "D-06: New test file is EventServiceTest.kt placed in workday-application/src/test/kotlin/community/flock/eco/workday/services/ alongside other service tests."
  artifacts:
    - path: "workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt"
      provides: "Integration test class covering ALLOC-04 type-swap semantics"
      contains: "class EventServiceTest"
      exports:
        - "@Test typeSwap or equivalently-named test"
  key_links:
    - from: "EventServiceTest"
      to: "EventService.syncBudgetAllocations"
      via: "eventService.update(existingCode, EventForm(...))"
      pattern: "eventService\\.update"
    - from: "EventServiceTest"
      to: "BudgetAllocationService.findAllByEventCode"
      via: "post-swap assertion"
      pattern: "findAllByEventCode"
---

<objective>
Add an integration test class `EventServiceTest.kt` that proves Plan 01's fix works. The test creates an event of type `FLOCK_HACK_DAY` (default time allocation type HACK_TIME), updates it to type `CONFERENCE` (default time allocation type STUDY_TIME), and asserts the persisted allocations were swapped from `HackTimeBudgetAllocation` to `StudyTimeBudgetAllocation`. A second test case covers D-03: when a participant already has BOTH a `HackTimeBudgetAllocation` AND a `StudyTimeBudgetAllocation` for the same event, the swap deletes both and leaves exactly one allocation of the target type.

Purpose: Provide a fast, deterministic JVM-level regression test for ALLOC-04 so the fix cannot silently regress. Required by D-05 and D-06.
Output: New file at `workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` containing at minimum two `@Test` methods that pass against the Plan 01 fix and fail against the pre-fix code.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-CONTEXT.md
@.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-PATTERNS.md

<interfaces>
<!-- Test base class and helper APIs the executor MUST use. Extracted from codebase. -->

From workday-application/src/test/kotlin/community/flock/eco/workday/WorkdayIntegrationTest.kt:
- `@SpringBootTest`, `@ActiveProfiles("test")`, `@AfterEach` cleanup. Extend with `: WorkdayIntegrationTest()`.

From workday-application/src/test/kotlin/community/flock/eco/workday/services/ContractServiceTest.kt (pattern model):
- Constructor injection: `class EventServiceTest(@Autowired private val ...) : WorkdayIntegrationTest()`.

From workday-application/src/test/kotlin/community/flock/eco/workday/helpers/CreateHelper.kt:
- `createHelper.createPerson()` (or `createPersonEntity()`) — produces a Person domain object whose `uuid` is reusable as an `EventForm.personIds` entry.
  (Patterns map note: `CreateHelper.createEvent` does NOT set `defaultTimeAllocationType`, so use `eventService.create(EventForm(...))` directly instead.)

From workday-application/src/main/kotlin/community/flock/eco/workday/application/forms/EventForm.kt and ../model/EventType.kt:
- `EventForm(description, from, to, hours, days, budget, personIds, type)` — exactly the constructor shape used in `EventControllerTest.kt` lines 77-91.
- `EventType.FLOCK_HACK_DAY` -> `defaultTimeAllocationType` resolves to HACK_TIME inside the service/form layer.
- `EventType.CONFERENCE` -> resolves to STUDY_TIME.

From workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt:
- `fun create(form: EventForm): Event`
- `fun update(code: String, form: EventForm): Event` (used to trigger the swap)

From domain/src/main/kotlin/community/flock/eco/workday/domain/budget/BudgetAllocationService.kt:
- `fun findAllByEventCode(eventCode: String): List<BudgetAllocation>` — line 15.

Sealed subtypes for assertion:
- `community.flock.eco.workday.domain.budget.HackTimeBudgetAllocation`
- `community.flock.eco.workday.domain.budget.StudyTimeBudgetAllocation`
- `community.flock.eco.workday.domain.budget.StudyMoneyBudgetAllocation` (only used to filter OUT of time-allocation assertions)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write EventServiceTest covering the HACK_TIME -> STUDY_TIME swap</name>
  <files>workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt</files>
  <read_first>
    - workday-application/src/test/kotlin/community/flock/eco/workday/services/ContractServiceTest.kt (constructor-injection class pattern, lines 1-36)
    - workday-application/src/test/kotlin/community/flock/eco/workday/WorkdayIntegrationTest.kt (base class annotations and cleanup behavior)
    - workday-application/src/test/kotlin/community/flock/eco/workday/helpers/CreateHelper.kt (createPerson / createPersonEntity signatures)
    - workday-application/src/test/kotlin/community/flock/eco/workday/controllers/EventControllerTest.kt (lines 77-91 — `EventForm` construction pattern with `type = EventType.X`)
    - workday-application/src/test/kotlin/community/flock/eco/workday/services/BudgetAllocationPersistenceTest.kt (filterIsInstance + assertIs/assertEquals/assertTrue assertion patterns)
    - workday-application/src/main/kotlin/community/flock/eco/workday/application/services/EventService.kt (confirm `create` and `update` signatures)
    - .planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-PATTERNS.md (sections "EventServiceTest.kt (new file)" and "BudgetAllocation assertion pattern")
  </read_first>
  <behavior>
    - Test `typeSwapFromHackToStudyDeletesHackAllocationAndCreatesStudyAllocation`:
      - GIVEN a `Person` created via `createHelper.createPerson()`.
      - GIVEN an event created via `eventService.create(EventForm(type = EventType.FLOCK_HACK_DAY, personIds = listOf(person.uuid), from = 2026-01-15, to = 2026-01-15, hours = 8.0, days = mutableListOf(8.0), budget = 200.0, description = "Type swap test"))`.
      - PRECONDITION: `budgetAllocationService.findAllByEventCode(event.code).filterIsInstance<HackTimeBudgetAllocation>()` has size 1; `.filterIsInstance<StudyTimeBudgetAllocation>()` is empty.
      - WHEN `eventService.update(event.code, EventForm(... same fields ..., type = EventType.CONFERENCE))` is called.
      - THEN `budgetAllocationService.findAllByEventCode(event.code).filterIsInstance<HackTimeBudgetAllocation>()` is empty.
      - THEN `budgetAllocationService.findAllByEventCode(event.code).filterIsInstance<StudyTimeBudgetAllocation>()` has size 1.
      - THEN the surviving `StudyTimeBudgetAllocation.totalHours` equals 8.0 (matches event days).
      - THEN money allocation count is unchanged (one `StudyMoneyBudgetAllocation` for the participant given budget=200.0).
    - Test `typeSwapWhenPersonHadBothTimeAllocationsCollapsesToSingleNewType` (covers D-03):
      - GIVEN the same setup as the first test through `eventService.create` with `EventType.FLOCK_HACK_DAY` (yields one `HackTimeBudgetAllocation`).
      - GIVEN an additional `StudyTimeBudgetAllocation` is created directly via `studyTimeBudgetAllocationService.create(...)` for the same `person`, same `eventCode`, same `date`, `description`, `dailyTimeAllocations`, `totalHours` — simulating the legitimate dual-allocation state per D-03.
      - PRECONDITION: time allocations for the event total 2 (one Hack + one Study).
      - WHEN `eventService.update(event.code, EventForm(... same fields ..., type = EventType.CONFERENCE))` is called.
      - THEN `findAllByEventCode(event.code).filterIsInstance<HackTimeBudgetAllocation>()` is empty.
      - THEN `findAllByEventCode(event.code).filterIsInstance<StudyTimeBudgetAllocation>()` has size exactly 1.
  </behavior>
  <action>
    Per D-05 and D-06 (Kotlin unit test required, file co-located under `services/` next to other service tests):

    Create `workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` with package `community.flock.eco.workday.services`.

    Class signature: `class EventServiceTest(@Autowired private val eventService: EventService, @Autowired private val budgetAllocationService: BudgetAllocationService, @Autowired private val studyTimeBudgetAllocationService: StudyTimeBudgetAllocationService, @Autowired private val createHelper: CreateHelper) : WorkdayIntegrationTest()`.

    Imports required: `community.flock.eco.workday.WorkdayIntegrationTest`, `community.flock.eco.workday.application.forms.EventForm`, `community.flock.eco.workday.application.model.EventType`, `community.flock.eco.workday.application.services.EventService`, `community.flock.eco.workday.domain.budget.BudgetAllocationService`, `community.flock.eco.workday.domain.budget.HackTimeBudgetAllocation`, `community.flock.eco.workday.domain.budget.StudyTimeBudgetAllocation`, `community.flock.eco.workday.domain.budget.StudyTimeBudgetAllocationService`, `community.flock.eco.workday.domain.budget.StudyMoneyBudgetAllocation`, `community.flock.eco.workday.helpers.CreateHelper`, `org.junit.jupiter.api.Test`, `org.springframework.beans.factory.annotation.Autowired`, `java.time.LocalDate`, `kotlin.test.assertEquals`, `kotlin.test.assertIs`, `kotlin.test.assertTrue`.

    Verify the actual `EventForm` field names match the production class before writing (some fields may be `personIds: List<UUID>` vs `Set<UUID>`; copy field names verbatim from `EventControllerTest.kt` and the form file). DO NOT invent constructor arguments. If a required field is missing from this plan's example, copy it from `EventControllerTest.kt::createEvent` lines 77-91 using the same literal value.

    Implement the two `@Test` methods exactly as described in `<behavior>` above. Use `assertEquals(0, ...)`, `assertEquals(1, ...)`, and `assertIs<StudyTimeBudgetAllocation>(timeAllocs.first())` for assertions. For the `dailyTimeAllocations` constructor argument required by the manual `studyTimeBudgetAllocationService.create(...)` call in test 2, reuse the values from the existing post-create allocation (re-read the Hack allocation via `findAllByEventCode`, copy its `dailyTimeAllocations` and `totalHours`).

    DO NOT add a third test for money allocation behavior — that is out of scope (Plan 01 explicitly does not touch money).

    DO NOT use `@Transactional` at the class level — `WorkdayIntegrationTest` already provides per-test cleanup.

    DO NOT introduce new helper methods in `CreateHelper.kt` — call its existing functions only. If a needed shape is missing, build the `EventForm` inline in the test as `EventControllerTest.kt` does.
  </action>
  <verify>
    <automated>./mvnw -pl workday-application test -Dtest=EventServiceTest -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt`.
    - `grep -n "package community.flock.eco.workday.services" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns exactly 1 match.
    - `grep -n ": WorkdayIntegrationTest()" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns at least 1 match.
    - `grep -cE "@Test" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns >= 2.
    - `grep -n "EventType.FLOCK_HACK_DAY" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns at least 1 match.
    - `grep -n "EventType.CONFERENCE" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns at least 1 match.
    - `grep -n "filterIsInstance<HackTimeBudgetAllocation>" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns at least 1 match.
    - `grep -n "filterIsInstance<StudyTimeBudgetAllocation>" workday-application/src/test/kotlin/community/flock/eco/workday/services/EventServiceTest.kt` returns at least 1 match.
    - `./mvnw -pl workday-application test -Dtest=EventServiceTest -q` exits 0 (both tests pass against Plan 01's fix).
    - Nyquist check: running the same command against `git stash` of Plan 01's `EventService.kt` produces at least one failing test (executor confirms via `git stash` + run + `git stash pop`, or by temporarily reverting the `filter` line and re-running). Document the failing test name and assertion in the SUMMARY.
  </acceptance_criteria>
  <done>
    `EventServiceTest.kt` exists with two passing `@Test` methods that exercise the type swap and the dual-allocation D-03 case. The full test class runs green against the Plan 01 fix and red against the pre-fix code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test JVM -> Spring context (H2) | Tests run in-process against the `test` profile with H2; no external network or filesystem trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-T1 | Tampering | Test data leakage between tests | mitigate | Extend `WorkdayIntegrationTest`; per-test DB reset via `cleanupDbService.cleanup()` is already wired (see `WorkdayIntegrationTest.kt` lines 20-45). Do NOT add `@Transactional` (would disable real persistence we need to verify). |
| T-18.1-T2 | Information Disclosure | Test logs leak credentials | accept | Tests use generated test persons via `CreateHelper`; no real PII or secrets are written. Standard project policy. |
| T-18.1-T3 | Repudiation | False positive in test | mitigate | Nyquist gate (acceptance criterion): test must demonstrably fail against the pre-fix `EventService.kt`. Executor runs the test once against the stash of the old code and records the failure mode in the SUMMARY. |
</threat_model>

<verification>
- `./mvnw -pl workday-application test -Dtest=EventServiceTest -q` exits 0.
- Nyquist check executed: with Plan 01 reverted (e.g., via `git stash` on `EventService.kt`), at least one `@Test` in `EventServiceTest` fails — proving the test actually exercises the fix.
- No new compilation warnings beyond baseline.
</verification>

<success_criteria>
- ALLOC-04 has a fast (< 30s) JVM-level regression test.
- The test fails against the pre-fix code (Nyquist valid) and passes against Plan 01's fix.
- Test file lives next to existing service tests at `workday-application/src/test/kotlin/community/flock/eco/workday/services/`.
- Covers both the basic HACK -> STUDY swap and the D-03 dual-allocation case.
</success_criteria>

<output>
Create `.planning/phases/18.1-close-gap-alloc-04-swap-allocation-type-on-event-type-change/18.1-02-SUMMARY.md` when done, including:
- The two `@Test` method names and a one-line description of each
- The exact `./mvnw` command used and its passing output (last 5 lines)
- Nyquist check: the test name that failed against the pre-fix code and the assertion that surfaced the regression
</output>
</content>
</invoke>
