---
phase: 15-event-allocation-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workday-application/src/main/react/features/event/eventBudgetTransformers.ts
  - workday-application/src/main/react/features/event/eventBudgetTransformers.test.ts
  - workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx
  - workday-application/src/main/react/features/event/EventBudgetSummaryBanner.test.tsx
  - workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx
autonomous: true
gap_closure: true
requirements:
  - ALLOC-03
  - SUMM-01

must_haves:
  truths:
    - "generateDefaultAllocations returns empty moneyParticipants when invoked with GENERAL_EVENT or FLOCK_COMMUNITY_DAY"
    - "generateDefaultAllocations still produces equal-share moneyParticipants when invoked with FLOCK_HACK_DAY or CONFERENCE and a positive budget"
    - "EventBudgetSummaryBanner collapsed-mode hides the money summary text and the Budget/Allocated chips when the event type is GENERAL_EVENT or FLOCK_COMMUNITY_DAY, even if a totalBudget value is passed"
    - "EventBudgetManagementDialog passes formValues.type into EventBudgetSummaryBanner so the collapsed summary matches the inner showMoneySection guard"
  artifacts:
    - path: "workday-application/src/main/react/features/event/eventBudgetTransformers.ts"
      provides: "generateDefaultAllocations with eventType parameter gating moneyParticipants on EventTypeMappingToDefaultBudgetType[eventType] != null"
      contains: "EventTypeMappingToDefaultBudgetType"
    - path: "workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx"
      provides: "EventBudgetSummaryBannerProps with optional eventType prop; hasMoneySection derived from eventType + hasBudget"
      contains: "eventType"
    - path: "workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx"
      provides: "EventBudgetSummaryBanner usage threaded with eventType={formValues.type}"
      contains: "eventType={formValues.type}"
  key_links:
    - from: "eventBudgetTransformers.ts:generateDefaultAllocations"
      to: "utils/mappings.ts:EventTypeMappingToDefaultBudgetType"
      via: "lookup keyed by eventType, falsy result -> moneyParticipants = []"
      pattern: "EventTypeMappingToDefaultBudgetType\\["
    - from: "EventBudgetManagementDialog.tsx:EventBudgetSummaryBanner usage"
      to: "EventBudgetSummaryBanner.tsx:hasMoneySection"
      via: "eventType prop"
      pattern: "eventType=\\{formValues.type\\}"
---

<objective>
Close two diagnosed gaps from Phase 15 UAT Test 3 — money allocations and the collapsed-mode budget pills both show up for event types that should have no money budget (GENERAL_EVENT, FLOCK_COMMUNITY_DAY).

Purpose: Make the canonical default-allocation generator and the collapsed banner agree with the existing `showMoneySection` guard already used inside `EventBudgetManagementDialog` (`formValues.type === FLOCK_HACK_DAY || === CONFERENCE`). Single source of truth is `EventTypeMappingToDefaultBudgetType` plus that boolean.

Output: Updated `generateDefaultAllocations`, updated `EventBudgetSummaryBanner`, updated caller wiring, plus updated unit tests covering the new branches.
</objective>

<execution_context>
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-event-allocation-persistence/15-UAT.md
@.planning/phases/15-event-allocation-persistence/15-CONTEXT.md
@.planning/phases/15-event-allocation-persistence/15-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do not explore the codebase to rediscover these. -->

From workday-application/src/main/react/utils/mappings.ts:
- `EventBudgetType = { STUDY: 'STUDY', HACK: 'HACK' }` (UI-level)
- `EventTypeMappingToDefaultBudgetType: Record<EventType, EventBudgetType | null>` mapping:
  - `GENERAL_EVENT -> null`
  - `FLOCK_HACK_DAY -> HACK`
  - `FLOCK_COMMUNITY_DAY -> null`
  - `CONFERENCE -> STUDY`

From workday-application/src/main/react/clients/EventClient.ts:
- `enum EventType { GENERAL_EVENT, FLOCK_HACK_DAY, FLOCK_COMMUNITY_DAY, CONFERENCE }`

From workday-application/src/main/react/features/event/eventBudgetTransformers.ts (current signature — to be CHANGED):
```
export function generateDefaultAllocations(
  personIds: string[],
  persons: Array<{ uuid: string; firstname: string; lastname: string }>,
  eventFrom: Dayjs,
  days: number[],
  defaultBudgetType: EventBudgetType | null,
  totalBudget: number,
): { timeParticipants: PersonTimeAllocation[]; moneyParticipants: PersonMoneyAllocation[] }
```

From workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx:
- Line 92 already encodes the canonical guard: `const showMoneySection = formValues.type === EventType.FLOCK_HACK_DAY || formValues.type === EventType.CONFERENCE;`
- Line 425 instantiates `<EventBudgetSummaryBanner totalBudget={formValues.budget} totalAllocated={totalMoneyAllocated} totalTime={totalTimeAllocated} participantCount={participantIds.length} defaultHoursPerDay={defaultHoursPerDay} defaultBudgetType={formValues.defaultTimeAllocationType} hasUnsavedChanges={isDirty} />` — this is the ONE callsite where the new prop must be wired.

From workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx:
- Current line 56: `const hasMoneySection = hasBudget;` — must become a function of both `hasBudget` AND event type.
- Banner has both a collapsed (AccordionSummary) branch and an expanded Alert branch. Only the collapsed branch is affected by gap 2 (the expanded branch never renders for the dialog's collapsed view, but treat the new guard consistently so the expanded view also hides money sections for non-money event types).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate moneyParticipants on eventType inside generateDefaultAllocations</name>
  <files>
    workday-application/src/main/react/features/event/eventBudgetTransformers.ts
    workday-application/src/main/react/features/event/eventBudgetTransformers.test.ts
  </files>
  <read_first>
    - workday-application/src/main/react/features/event/eventBudgetTransformers.ts (lines 1-60 for imports, 295-365 for current `generateDefaultAllocations` and the surrounding helpers)
    - workday-application/src/main/react/features/event/eventBudgetTransformers.test.ts (lines 1-50 for imports + test setup, 570-720 covering the `generateDefaultAllocations` describe block and all six existing test cases)
    - workday-application/src/main/react/utils/mappings.ts (full file — 33 lines; the lookup table being introduced as the gating source)
    - workday-application/src/main/react/clients/EventClient.ts (search for `enum EventType` and the form/contract types to confirm exported name and member identifiers)
  </read_first>
  <behavior>
    - Test 1 (HACK_TIME path — existing, keep): `generateDefaultAllocations` with `eventType=FLOCK_HACK_DAY`, two personIds, positive `totalBudget` returns two `moneyParticipants` with equal floored share.
    - Test 2 (CONFERENCE path — existing, keep): same as Test 1 with `eventType=CONFERENCE` and `defaultBudgetType=STUDY` — moneyParticipants populated.
    - Test 3 (NEW, gap 1): `eventType=GENERAL_EVENT`, two personIds, `totalBudget=600` -> `moneyParticipants` is `[]`. `timeParticipants` still produced according to `defaultBudgetType` (which will be `null` for GENERAL_EVENT in real use, but the function must not assume that — pass null in this case).
    - Test 4 (NEW, gap 1): `eventType=FLOCK_COMMUNITY_DAY`, two personIds, `totalBudget=600` -> `moneyParticipants` is `[]`.
    - Test 5 (existing, keep): zero budget still produces moneyParticipants with `amount=0` for HACK_DAY / CONFERENCE (no behavior change for money-eligible types).
    - Test 6 (existing, keep): single person / floor-rounding case for HACK_DAY remains unchanged.
    - All six existing tests must be updated to pass an `eventType` argument matching their `defaultBudgetType` (FLOCK_HACK_DAY for HACK, CONFERENCE for STUDY).
  </behavior>
  <action>
    Change the `generateDefaultAllocations` signature to accept a new required `eventType: EventType` parameter. Decide on argument order: append `eventType` as the LAST parameter so existing call positions don't silently misalign — this matters because there are no production callers yet (per STATE.md), but the test file passes positional args. Inside the function, import `EventTypeMappingToDefaultBudgetType` from `../../utils/mappings` (and `EventType` from `../clients/EventClient` if not already imported) and compute `const isMoneyEligible = EventTypeMappingToDefaultBudgetType[eventType] !== null;`. When `isMoneyEligible` is `false`, return `moneyParticipants: []` (skip the existing `personIds.map(...)` block — do not allocate per-person zero-amount rows). When `true`, retain the existing equal-share floor logic exactly as today.

    Do NOT change `timeParticipants` generation — it is correctly gated by the existing `defaultBudgetType` parameter (HACK / STUDY / null). The bug is only in the money branch.

    Update `eventBudgetTransformers.test.ts`: every existing call to `generateDefaultAllocations(...)` in the describe block at line 576 must thread the new `eventType` argument. Pick `EventType.FLOCK_HACK_DAY` for tests using `defaultBudgetType='HACK'` and `EventType.CONFERENCE` for tests using `defaultBudgetType='STUDY'`. Add the two new test cases (Test 3 and Test 4 in `<behavior>`) asserting `moneyParticipants).toEqual([])`. Keep test descriptions short and prefixed with `gap closure:` for the two new cases so they are greppable.

    Do not introduce a default value for `eventType`; making it required forces the type system to flag any future caller that forgets to pass it.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="eventBudgetTransformers" 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `generateDefaultAllocations` signature accepts `eventType: EventType` as the final required parameter.
    - `grep -nE "EventTypeMappingToDefaultBudgetType\[eventType\]" workday-application/src/main/react/features/event/eventBudgetTransformers.ts` returns at least one match.
    - All eight tests in the `describe('generateDefaultAllocations', ...)` block pass (six updated + two new).
    - `npm test -- --testPathPattern="eventBudgetTransformers"` exits with status 0.
    - No production code outside the test file invokes `generateDefaultAllocations` (`grep -rn "generateDefaultAllocations(" workday-application/src/main/react/ --include="*.ts" --include="*.tsx" | grep -v ".test."` returns no results — confirms cleanup item in STATE.md is still accurate and that the new required arg cannot break runtime callers).
  </done>
  <acceptance_criteria>
    - For inputs `(personIds=[A,B], persons=[...], from=2026-01-01, days=[8,8], defaultBudgetType=null, totalBudget=600, eventType=GENERAL_EVENT)` the return shape is `{ timeParticipants: [<two entries with hackPeriod=null, studyPeriod=null>], moneyParticipants: [] }`.
    - For the same inputs with `eventType=FLOCK_COMMUNITY_DAY`, `moneyParticipants` is also `[]`.
    - For `eventType=FLOCK_HACK_DAY` with `defaultBudgetType='HACK'`, `totalBudget=600`, and two personIds, `moneyParticipants` is `[{ personId:A, personName:..., amount:300 }, { personId:B, personName:..., amount:300 }]`.
    - For `eventType=CONFERENCE` with `defaultBudgetType='STUDY'`, the same equal-share behaviour holds.
    - Test file's existing six cases continue to pass after the signature update.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add eventType prop to EventBudgetSummaryBanner and wire it from EventBudgetManagementDialog</name>
  <files>
    workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx
    workday-application/src/main/react/features/event/EventBudgetSummaryBanner.test.tsx
    workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx
  </files>
  <read_first>
    - workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx (entire file — 169 lines)
    - workday-application/src/main/react/features/event/EventBudgetSummaryBanner.test.tsx (entire file — all 11 test cases referenced by grep) — needed to know how each existing case constructs props so the new optional `eventType` doesn't regress assertions
    - workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx (lines 1-100 for imports and the `showMoneySection` definition at line 92; lines 415-440 for the `<EventBudgetSummaryBanner>` callsite)
    - workday-application/src/main/react/utils/mappings.ts (already read for Task 1 — referenced here to confirm import path if Banner consumes the mapping table directly)
    - workday-application/src/main/react/clients/EventClient.ts (confirm `EventType` enum export path used by Banner)
  </read_first>
  <behavior>
    - Test A (NEW, gap 2): Collapsed-mode banner with `participantCount=2, totalBudget=600, totalAllocated=600, eventType=FLOCK_HACK_DAY` -> rendered text contains `assigned €300/person` AND the Budget/Allocated chips are rendered (existing behaviour preserved for money-eligible types).
    - Test B (NEW, gap 2): Same props but `eventType=GENERAL_EVENT` -> rendered text does NOT contain `assigned`, does NOT contain `unassigned`, does NOT contain `over budget`, and the Budget/Allocated chips are NOT rendered. If `defaultBudgetType` is also null/undefined, the fallback `no budget allocations for this event type` message renders.
    - Test C (NEW, gap 2): `eventType=FLOCK_COMMUNITY_DAY, totalBudget=600, defaultBudgetType=null` -> same as Test B (no money UI).
    - Test D (NEW, gap 2): `eventType=CONFERENCE, totalBudget=900, totalAllocated=600, participantCount=2` -> text contains `assigned €300/person, €300 unassigned`.
    - Test E (existing collapsed-mode test that omits `eventType`): when `eventType` is undefined (legacy callers), fall back to the current behavior (`hasMoneySection = hasBudget`) so the 11 existing tests continue to pass without modification. Document this fallback in a code comment.
    - Test F (existing expanded-mode tests): unaffected — the Alert-based expanded branch is rendered when `participantCount` is undefined and the existing assertions about chip rendering must still hold without changes.
  </behavior>
  <action>
    Add `eventType?: EventType` to `EventBudgetSummaryBannerProps` in `EventBudgetSummaryBanner.tsx`. Import `EventType` from `../clients/EventClient` and `EventTypeMappingToDefaultBudgetType` from `../../utils/mappings`. In the collapsed-mode branch (inside the `if (isCollapsedMode)` block, currently lines 50-116), replace `const hasMoneySection = hasBudget;` (line 56) with a derivation that requires BOTH (a) `hasBudget` AND (b) when `eventType` is defined, `EventTypeMappingToDefaultBudgetType[eventType] !== null`. Concretely: `const isMoneyEligibleType = eventType === undefined ? true : EventTypeMappingToDefaultBudgetType[eventType] !== null; const hasMoneySection = hasBudget && isMoneyEligibleType;`. The `eventType === undefined ? true` fallback preserves backwards compatibility for the 11 existing tests that never pass `eventType`.

    Do NOT modify the rest of the collapsed-mode rendering logic — the existing `if (hasMoneySection)` blocks for summary text and the Budget/Allocated chip group (lines 65-74 for text; lines 96-113 for chips) will now correctly skip when the type is GENERAL_EVENT or FLOCK_COMMUNITY_DAY because `hasMoneySection` is false. Verify the fallback text on lines 76-78 (`'no budget allocations for this event type'`) still triggers when both `hasTimeSection` and `hasMoneySection` are false; this is the desired UX for GENERAL_EVENT with `defaultBudgetType=null`.

    In `EventBudgetManagementDialog.tsx`, update the `<EventBudgetSummaryBanner ... />` invocation at line 425 to thread the new prop: add `eventType={formValues.type}`. Do not modify the existing `showMoneySection` const at line 92 — it stays as the in-dialog content guard.

    In `EventBudgetSummaryBanner.test.tsx`, ADD (do not modify) four new test cases (Test A through D in `<behavior>`) inside the existing `describe('EventBudgetSummaryBanner', ...)` block. Use the same render setup pattern as existing tests and import `EventType` from `../clients/EventClient`. Use `screen.queryByText` / `screen.getByText` regex patterns consistent with surrounding tests. Do not touch the 11 existing tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="EventBudgetSummaryBanner" 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `grep -nE "eventType\?: EventType" workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx` matches at least once (interface field).
    - `grep -nE "EventTypeMappingToDefaultBudgetType\[eventType\]" workday-application/src/main/react/features/event/EventBudgetSummaryBanner.tsx` matches at least once (the new gating expression).
    - `grep -nE "eventType=\{formValues\.type\}" workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx` matches exactly once (the Banner callsite).
    - The four new test cases pass and the 11 existing tests still pass: `npm test -- --testPathPattern="EventBudgetSummaryBanner"` exits status 0.
    - `npm test -- --testPathPattern="event/" 2>&1 | tail -10` shows no regressions in adjacent event tests.
  </done>
  <acceptance_criteria>
    - When `EventBudgetSummaryBanner` is rendered with `eventType=GENERAL_EVENT, totalBudget=600, totalAllocated=0, participantCount=2, defaultBudgetType=null`, the rendered DOM contains the substring `no budget allocations for this event type` and does NOT contain the substring `assigned`, `unassigned`, or `€600` in the chip group.
    - When rendered with `eventType=FLOCK_HACK_DAY, totalBudget=600, totalAllocated=600, participantCount=2`, the DOM contains `assigned €300/person` and renders the `Budget: €600` and `Allocated: €600` chips.
    - When rendered with no `eventType` prop and `totalBudget=600, participantCount=2`, behavior is identical to before this change (the 11 existing snapshot/assertion tests pass unmodified).
    - The single callsite in `EventBudgetManagementDialog.tsx` (line ~425) passes `eventType={formValues.type}`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event form -> default-allocation generator | Form values (including `type`) flow into pure transformer logic; no untrusted external input crosses here (admin-only UI). |
| Dialog -> summary banner | Props pass internally inside the same React tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-01 | Tampering | `generateDefaultAllocations` consumers | accept | No production callers exist (per STATE.md); function is currently test-only. Adding a required `eventType` arg is type-system enforced, preventing accidental misuse when re-wired in the future. |
| T-15.2-02 | Information Disclosure | EventBudgetSummaryBanner | accept | No PII or sensitive amounts are introduced; the change SUPPRESSES display of money for non-money event types, which is the desired behaviour. |
| T-15.2-03 | Denial of Service | Banner re-render | accept | New prop reads a constant lookup table; O(1) and side-effect-free. |
</threat_model>

<verification>
- Run unit tests for the two affected modules:
  - `npm test -- --testPathPattern="eventBudgetTransformers"`
  - `npm test -- --testPathPattern="EventBudgetSummaryBanner"`
- Both must pass with the new + existing cases.
- Quick smoke check (manual, not a checkpoint — UAT will re-run Test 3 after merge):
  1. `npm start`, open an event with `type=GENERAL_EVENT` and `budget>0` -> banner collapsed text shows participant count but no `assigned` / `unassigned` and no Budget/Allocated chips.
  2. Save the event -> no money allocations are created for participants on their Budget Allocation tab.
  3. Change the same event's type to `FLOCK_HACK_DAY` -> banner shows `assigned €X/person`, chips reappear; saving creates equal-share study money allocations.
</verification>

<success_criteria>
- Both gaps from `15-UAT.md` closed:
  - Gap 1 (major): `generateDefaultAllocations` returns empty `moneyParticipants` for GENERAL_EVENT and FLOCK_COMMUNITY_DAY; equal-share preserved for FLOCK_HACK_DAY and CONFERENCE.
  - Gap 2 (minor): Banner's collapsed-mode `hasMoneySection` is false when `eventType` is GENERAL_EVENT or FLOCK_COMMUNITY_DAY, hiding the money summary text and the Budget/Allocated chips.
- Single source of truth: both behaviours derive from `EventTypeMappingToDefaultBudgetType` (the same table already encoding null for non-money types).
- No production callers of `generateDefaultAllocations` are broken (it is currently test-only per STATE.md).
- `EventBudgetSummaryBanner`'s 11 existing tests continue to pass unmodified (new `eventType` prop is optional with backwards-compat fallback).
- The existing `showMoneySection` guard in `EventBudgetManagementDialog.tsx` line 92 remains the in-dialog content gate; the new prop wiring at line 425 brings the collapsed-summary view into alignment with that same logic.
</success_criteria>

<output>
Create `.planning/phases/15-event-allocation-persistence/15-02-SUMMARY.md` when done.
</output>
