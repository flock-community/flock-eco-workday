---
phase: 16-budget-allocation-list-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workday-application/src/main/react/features/budget/EventAllocationListItem.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03

must_haves:
  truths:
    - "Admin clicking an event allocation row link navigates to /event?code={eventCode}"
    - "Employee sees event name as plain non-clickable text (no link, no icon)"
    - "Filter chips (All / Hack Hours / Study Hours / Study Money) are present and toggle the allocation list"
    - "Event-linked allocations show the event description name, not the raw event code"
  artifacts:
    - path: "workday-application/src/main/react/features/budget/EventAllocationListItem.tsx"
      provides: "Admin event link with correct ?code= query param"
      contains: "href={`/event?code=${eventCode}`}"
  key_links:
    - from: "EventAllocationListItem.tsx"
      to: "/event?code={eventCode}"
      via: "MUI Link href prop"
      pattern: "href=.*event.*code.*eventCode"
---

<objective>
Fix the admin event link in the budget allocation list so it carries the event code as a query parameter, enabling future deep-link navigation to the correct event dialog.

Purpose: Admin should land on /event?code=XYZ when clicking an event allocation, not the bare /event page. This prepares the URL for GitHub issue #458 (EventFeature consuming the ?code= param). LIST-01 (event names) and LIST-03 (filter chips) are already implemented — this plan addresses only LIST-02.

Output: EventAllocationListItem.tsx with href updated from `/event` to `/event?code=${eventCode}` at line 148.
</objective>

<execution_context>
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-budget-allocation-list-ux/16-CONTEXT.md
@.planning/phases/16-budget-allocation-list-ux/16-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From workday-application/src/main/react/features/budget/EventAllocationListItem.tsx (lines 26-30):
```typescript
interface EventAllocationListItemProps {
  eventName: string;
  eventCode: string;  // <-- this is the prop to use in the href
  allocations: BudgetAllocation[];
  isAdmin?: boolean;
  // ...
}
```

Current line 148 (the only change):
```tsx
<Link href="/event" underline="hover" sx={{display: 'inline-flex', alignItems: 'center', gap: 0.5}}>
```

Required state after fix:
```tsx
<Link href={`/event?code=${eventCode}`} underline="hover" sx={{display: 'inline-flex', alignItems: 'center', gap: 0.5}}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin event link href to include ?code= query param</name>
  <files>workday-application/src/main/react/features/budget/EventAllocationListItem.tsx</files>
  <read_first>
    - workday-application/src/main/react/features/budget/EventAllocationListItem.tsx — read the full file before touching anything; confirm current state at line 148 and verify `eventCode` is destructured from props
  </read_first>
  <action>
    In EventAllocationListItem.tsx, change line 148 from:

      `<Link href="/event" underline="hover" sx={{display: 'inline-flex', alignItems: 'center', gap: 0.5}}>`

    to:

      `<Link href={`/event?code=${eventCode}`} underline="hover" sx={{display: 'inline-flex', alignItems: 'center', gap: 0.5}}>`

    This is the only change in this file. Do NOT change the employee branch (the `eventName` plain text rendered when `isAdmin` is false), do NOT change the icon, do NOT change the sx props.

    Per user decision D-02 in 16-CONTEXT.md: The receiving side (EventFeature) does NOT need to consume this param yet — that is deferred to GitHub issue #458. The link opens in the same tab (no target="_blank").

    Note: LIST-01 (event names displayed via `eventName` prop) and LIST-03 (filter chips) are already correctly implemented — do not touch BudgetAllocationFeature.tsx or BudgetAllocationList.tsx.
  </action>
  <verify>
    <automated>grep -n 'href.*event.*code.*eventCode' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EventAllocationListItem.tsx line 148 contains: `href={\`/event?code=${eventCode}\`}`
    - EventAllocationListItem.tsx still contains `eventCode: string` in the props interface (line ~28)
    - EventAllocationListItem.tsx still contains the employee branch rendering plain `{eventName}` text (not wrapped in Link)
    - grep output: `grep -n 'href.*event.*code' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx` returns at least one match on line 148
    - grep output: `grep -c 'href="/event"' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx` returns `0` (old bare href is gone)
    - TypeScript build passes: `npm run yolo` exits 0 (or `npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Line 148 uses `href={\`/event?code=${eventCode}\`}` and the file compiles without TypeScript errors.</done>
</task>

</tasks>

<verification>
After the task completes, verify all three requirements are satisfied:

LIST-01: `grep -n 'eventName' workday-application/src/main/react/features/budget/BudgetAllocationList.tsx` — confirms eventName is passed from eventNameMap (already implemented, no change needed).

LIST-02: `grep -n 'href.*event.*code' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx` — confirms admin link includes ?code= param.

LIST-03: `grep -n 'typeFilter\|Hack Hours\|Study Hours\|Study Money' workday-application/src/main/react/features/budget/BudgetAllocationFeature.tsx` — confirms filter chips already implemented.
</verification>

<success_criteria>
1. `grep 'href=.*event.*code.*eventCode' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx` returns a match.
2. `grep -c 'href="/event"' workday-application/src/main/react/features/budget/EventAllocationListItem.tsx` returns `0`.
3. TypeScript compiles without errors.
4. Employee branch still renders plain text (no Link wrapping eventName for non-admin users).
</success_criteria>

<output>
After completion, create `.planning/phases/16-budget-allocation-list-ux/16-01-SUMMARY.md` following the summary template.
</output>
