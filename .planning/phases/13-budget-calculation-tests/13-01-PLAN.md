---
phase: 13-budget-calculation-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03

must_haves:
  truths:
    - "Two hack-time allocations in the same year sum correctly: budget minus both allocations' totalHours equals available"
    - "Creating a study-time allocation does not change the hackHours or studyMoney available values"
    - "An allocation dated 2025 is excluded when querying year=2026; an allocation dated 2026 is included"
  artifacts:
    - path: "workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt"
      provides: "Three new @Test methods covering CALC-01, CALC-02, CALC-03"
      contains: "multiple allocations sum correctly"
  key_links:
    - from: "BudgetSummaryControllerTest.kt"
      to: "GET /api/budget-summary?personId=&year="
      via: "MockMvc GET with asyncDispatch"
      pattern: "get.*budget-summary.*personId"
---

<objective>
Add three SpringBootTest methods to BudgetSummaryControllerTest covering the three CALC requirements:
- CALC-01: multiple allocations of the same type sum correctly
- CALC-02: allocation types are independent of each other
- CALC-03: year scoping excludes allocations from other years

Purpose: These are the only gaps in test coverage for Phase 13. All three hit the live GET /api/budget-summary endpoint through MockMvc, matching the established pattern in the file.

Output: Three new @Test methods in BudgetSummaryControllerTest.kt, all passing under ./mvnw test.
</objective>

<execution_context>
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-budget-calculation-tests/13-CONTEXT.md

<!-- Key interfaces the executor needs -->
<interfaces>
<!-- From BudgetSummaryControllerTest.kt (existing pattern to replicate) -->

// Class-level setup already present:
//   @Autowired mvc: MockMvc
//   @Autowired createHelper: CreateHelper
//   @Autowired hackTimeBudgetAllocationService: HackTimeBudgetAllocationService
//   @Autowired studyTimeBudgetAllocationService: StudyTimeBudgetAllocationService
//   @Autowired studyMoneyBudgetAllocationService: StudyMoneyBudgetAllocationService
//   val baseUrl = "/api/budget-summary"
//   val adminAuthorities = setOf(BudgetAllocationAuthority.READ, WRITE, ADMIN)
//   private fun ResultActions.asyncDispatch() = mvc.perform(MockMvcRequestBuilders.asyncDispatch(this.andReturn()))

// GET pattern:
//   mvc.perform(
//     MockMvcRequestBuilders.get("$baseUrl?personId=${person.uuid}&year=2026")
//       .with(SecurityMockMvcRequestPostProcessors.user(CreateHelper.UserSecurity(user)))
//       .accept(MediaType.APPLICATION_JSON)
//   ).asyncDispatch()
//    .andExpect(MockMvcResultMatchers.status().isOk)
//    .andExpect(MockMvcResultMatchers.jsonPath("$.hackHours.available").value(92.0))

// createContractInternal signature:
//   createHelper.createContractInternal(
//     person = personEntity,          // PersonEntity (createPersonEntity returns this)
//     from = LocalDate.of(2026,1,1),
//     to = LocalDate.of(2026,12,31),
//     hackHours = 100,
//     studyHours = 80,
//     studyMoney = BigDecimal("2500.00"),
//   )

// HackTimeBudgetAllocation constructor:
//   HackTimeBudgetAllocation(
//     person = person,                 // Person domain object (personEntity.toDomain())
//     eventCode = null,
//     date = LocalDate.of(2026, 3, 1),
//     description = "...",
//     dailyTimeAllocations = listOf(
//       DailyTimeAllocation(LocalDate.of(2026, 3, 1), 8.0, BudgetAllocationType.HACK)
//     ),
//     totalHours = 8.0,
//   )

// StudyMoneyBudgetAllocation constructor:
//   StudyMoneyBudgetAllocation(
//     person = person,
//     eventCode = null,
//     date = LocalDate.of(2026, 3, 3),
//     description = "...",
//     amount = BigDecimal("500.00"),
//   )
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CALC-01 test — multiple allocations of same type sum correctly</name>
  <files>workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt</files>
  <read_first>
    - workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt
  </read_first>
  <behavior>
    - Contract: hackHours=100, studyHours=0, studyMoney=0
    - Create two HackTimeBudgetAllocation: first with totalHours=8.0 (date 2026-03-01), second with totalHours=12.0 (date 2026-04-01)
    - GET /api/budget-summary?personId=...&year=2026
    - Assert hackHours.budget=100.0, hackHours.used=20.0, hackHours.available=80.0
    - Assert studyHours.used=0.0, studyMoney.used=0.0 (not affected)
  </behavior>
  <action>
    Append a new @Test method to BudgetSummaryControllerTest named:
    `multiple hack time allocations sum correctly for CALC-01`

    Implementation:
    1. Create user with adminAuthorities
    2. val personEntity = createHelper.createPersonEntity("calc01", "multi", user.code)
    3. val person = personEntity.toDomain()
    4. createHelper.createContractInternal(person=personEntity, from=LocalDate.of(2026,1,1), to=LocalDate.of(2026,12,31), hackHours=100, studyHours=0, studyMoney=BigDecimal.ZERO)
    5. hackTimeBudgetAllocationService.create(HackTimeBudgetAllocation(person=person, eventCode=null, date=LocalDate.of(2026,3,1), description="First hack day", dailyTimeAllocations=listOf(DailyTimeAllocation(LocalDate.of(2026,3,1), 8.0, BudgetAllocationType.HACK)), totalHours=8.0))
    6. hackTimeBudgetAllocationService.create(HackTimeBudgetAllocation(person=person, eventCode=null, date=LocalDate.of(2026,4,1), description="Second hack day", dailyTimeAllocations=listOf(DailyTimeAllocation(LocalDate.of(2026,4,1), 12.0, BudgetAllocationType.HACK)), totalHours=12.0))
    7. GET $baseUrl?personId=${person.uuid}&year=2026 with adminAuthorities user
    8. Assert: hackHours.budget=100.0, hackHours.used=20.0, hackHours.available=80.0, studyHours.used=0.0, studyMoney.used=0.0
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/workday-application && ../mvnw test -pl . -Dtest=BudgetSummaryControllerTest#"multiple hack time allocations sum correctly for CALC-01" -q 2>&1 | tail -20</automated>
  </verify>
  <done>Test method exists in the file and passes: hackHours.used=20.0, hackHours.available=80.0 for two combined hack allocations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CALC-02 test — allocation types are independent</name>
  <files>workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt</files>
  <read_first>
    - workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt
  </read_first>
  <behavior>
    - Contract: hackHours=100, studyHours=80, studyMoney=BigDecimal("2500.00")
    - Create only a StudyTimeBudgetAllocation: totalHours=40.0
    - GET /api/budget-summary?personId=...&year=2026
    - Assert studyHours.used=40.0, studyHours.available=40.0
    - Assert hackHours.used=0.0, hackHours.available=100.0 (study allocation did NOT affect hack)
    - Assert studyMoney.used=0.0, studyMoney.available=2500.0 (study allocation did NOT affect money)
  </behavior>
  <action>
    Append a new @Test method to BudgetSummaryControllerTest named:
    `study time allocation does not affect hack hours or study money for CALC-02`

    Implementation:
    1. Create user with adminAuthorities
    2. val personEntity = createHelper.createPersonEntity("calc02", "typeindep", user.code)
    3. val person = personEntity.toDomain()
    4. createHelper.createContractInternal(person=personEntity, from=LocalDate.of(2026,1,1), to=LocalDate.of(2026,12,31), hackHours=100, studyHours=80, studyMoney=BigDecimal("2500.00"))
    5. studyTimeBudgetAllocationService.create(StudyTimeBudgetAllocation(person=person, eventCode=null, date=LocalDate.of(2026,5,1), description="Study course", dailyTimeAllocations=listOf(DailyTimeAllocation(LocalDate.of(2026,5,1), 40.0, BudgetAllocationType.STUDY)), totalHours=40.0))
    6. GET $baseUrl?personId=${person.uuid}&year=2026 with adminAuthorities user
    7. Assert: studyHours.used=40.0, studyHours.available=40.0, hackHours.used=0.0, hackHours.available=100.0, studyMoney.used=0.0, studyMoney.available=2500.0
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/workday-application && ../mvnw test -pl . -Dtest=BudgetSummaryControllerTest#"study time allocation does not affect hack hours or study money for CALC-02" -q 2>&1 | tail -20</automated>
  </verify>
  <done>Test method exists in the file and passes: study allocation does not change hackHours.available=100.0 or studyMoney.available=2500.0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CALC-03 test — year scoping excludes other-year allocations</name>
  <files>workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt</files>
  <read_first>
    - workday-application/src/test/kotlin/community/flock/eco/workday/application/budget/BudgetSummaryControllerTest.kt
  </read_first>
  <behavior>
    - Contract covering 2025-01-01 to 2026-12-31, hackHours=100
    - Create HackTimeBudgetAllocation with date=2025-06-01, totalHours=50.0 (previous year)
    - Create HackTimeBudgetAllocation with date=2026-03-01, totalHours=20.0 (current year)
    - GET /api/budget-summary?personId=...&year=2026
    - Assert hackHours.used=20.0 (only the 2026 allocation is counted)
    - Assert hackHours.available=80.0 (100 - 20, the 2025 allocation is excluded)
  </behavior>
  <action>
    Append a new @Test method to BudgetSummaryControllerTest named:
    `allocations from different year are excluded from budget calculation for CALC-03`

    Implementation:
    1. Create user with adminAuthorities
    2. val personEntity = createHelper.createPersonEntity("calc03", "yearscope", user.code)
    3. val person = personEntity.toDomain()
    4. createHelper.createContractInternal(person=personEntity, from=LocalDate.of(2025,1,1), to=LocalDate.of(2026,12,31), hackHours=100, studyHours=0, studyMoney=BigDecimal.ZERO)
    5. hackTimeBudgetAllocationService.create(HackTimeBudgetAllocation(person=person, eventCode=null, date=LocalDate.of(2025,6,1), description="2025 hack day", dailyTimeAllocations=listOf(DailyTimeAllocation(LocalDate.of(2025,6,1), 50.0, BudgetAllocationType.HACK)), totalHours=50.0))
    6. hackTimeBudgetAllocationService.create(HackTimeBudgetAllocation(person=person, eventCode=null, date=LocalDate.of(2026,3,1), description="2026 hack day", dailyTimeAllocations=listOf(DailyTimeAllocation(LocalDate.of(2026,3,1), 20.0, BudgetAllocationType.HACK)), totalHours=20.0))
    7. GET $baseUrl?personId=${person.uuid}&year=2026 with adminAuthorities user
    8. Assert: hackHours.budget=100.0, hackHours.used=20.0, hackHours.available=80.0
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/workday-application && ../mvnw test -pl . -Dtest=BudgetSummaryControllerTest#"allocations from different year are excluded from budget calculation for CALC-03" -q 2>&1 | tail -20</automated>
  </verify>
  <done>Test method exists in the file and passes: hackHours.used=20.0 when queried for year=2026, even though a 50h allocation exists in 2025</done>
</task>

</tasks>

<verification>
Run the full BudgetSummaryControllerTest suite to confirm all existing tests still pass plus the three new ones:

```
cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday/workday-application && ../mvnw test -pl . -Dtest=BudgetSummaryControllerTest -q 2>&1 | tail -30
```

Expected output: 7 tests total (4 existing + 3 new), all passing, BUILD SUCCESS.
</verification>

<success_criteria>
- BudgetSummaryControllerTest.kt contains three new @Test methods with backtick-named functions ending in "for CALC-01", "for CALC-02", and "for CALC-03"
- All 7 tests in BudgetSummaryControllerTest pass: `Tests run: 7, Failures: 0, Errors: 0`
- CALC-01: hackHours.used=20.0 when two hack allocations of 8h and 12h are created
- CALC-02: hackHours.available=100.0 and studyMoney.available=2500.0 unchanged when only study hours are allocated
- CALC-03: hackHours.used=20.0 when queried for year=2026, 2025 allocation of 50h is excluded
</success_criteria>

<output>
After completion, create `.planning/phases/13-budget-calculation-tests/13-01-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`.
</output>
