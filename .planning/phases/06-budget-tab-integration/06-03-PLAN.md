---
phase: 06-budget-tab-integration
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - workday-application/src/main/react/features/budget/StudyMoneyAllocationDialog.tsx
  - workday-application/src/main/react/features/budget/BudgetAllocationFeature.tsx
  - workday-application/src/main/react/features/budget/mocks/BudgetAllocationTypes.ts
  - workday-application/src/main/react/features/budget/mocks/BudgetAllocationMocks.ts
  - workday-application/src/main/react/features/budget/BudgetAllocationDemo.tsx
autonomous: true
requirements:
  - TAB-03

must_haves:
  truths:
    - "Admin can open StudyMoney dialog, fill form, and create a new allocation that persists to database"
    - "Admin can delete a StudyMoney allocation with confirmation dialog and it is removed from database"
    - "After create or delete, the allocation list and summary cards refresh to reflect changes"
    - "All mock files and demo component are deleted"
  artifacts:
    - path: "workday-application/src/main/react/features/budget/StudyMoneyAllocationDialog.tsx"
      provides: "Dialog wired to real API for create/edit StudyMoney allocations"
      contains: "BudgetAllocationClient"
    - path: "workday-application/src/main/react/features/budget/BudgetAllocationFeature.tsx"
      provides: "Feature with delete confirmation dialog and create dialog integration"
      contains: "ConfirmDialog"
  key_links:
    - from: "StudyMoneyAllocationDialog.tsx"
      to: "BudgetAllocationClient.ts"
      via: "createStudyMoney call on form submit"
      pattern: "BudgetAllocationClient\\.createStudyMoney"
    - from: "BudgetAllocationFeature.tsx"
      to: "BudgetAllocationClient.ts"
      via: "deleteById call on confirm"
      pattern: "BudgetAllocationClient\\.deleteById"
    - from: "BudgetAllocationFeature.tsx"
      to: "ConfirmDialog"
      via: "import from @workday-core"
      pattern: "ConfirmDialog"
---

<objective>
Wire StudyMoney CRUD operations to real API, add delete confirmation dialog, and clean up all prototype artifacts (mock files, demo component).

Purpose: Enables admins to create and delete standalone StudyMoney allocations from the Budget tab (TAB-03). Removes all Phase 1 prototype scaffolding that is no longer needed.
Output: Working StudyMoney create/delete flow with confirmation, all mock files deleted.
</objective>

<execution_context>
@/Users/julius.van.dis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-budget-tab-integration/06-CONTEXT.md
@.planning/phases/06-budget-tab-integration/06-RESEARCH.md
@.planning/phases/06-budget-tab-integration/06-01-SUMMARY.md

@workday-application/src/main/react/features/budget/StudyMoneyAllocationDialog.tsx
@workday-application/src/main/react/features/budget/BudgetAllocationFeature.tsx
@workday-application/src/main/react/clients/BudgetAllocationClient.ts

<interfaces>
<!-- From Plan 02 (BudgetAllocationClient) -->
```typescript
export const BudgetAllocationClient = {
  findAll: (personId?: string, year?: number, eventCode?: string) => Promise<BudgetAllocation[]>,
  getSummary: (personId?: string, year?: number) => Promise<BudgetSummaryResponse>,
  createStudyMoney: (input: StudyMoneyAllocationInput) => Promise<BudgetAllocation>,
  deleteById: (id: string) => Promise<void>,
  uploadFile: (file: File) => Promise<{ id: string; name: string }>,
  downloadFile: (fileId: string) => string,
};
```

<!-- From wirespec/model (StudyMoneyAllocationInput) -->
```typescript
export interface StudyMoneyAllocationInput {
  personId: string;    // UUID
  eventCode?: string;
  date: string;        // ISO date
  description?: string;
  amount: number;
  files: BudgetAllocationFile[];
}
export interface BudgetAllocationFile {
  name: string;
  file: string;  // UUID
}
```

<!-- From @workday-core ConfirmDialog -->
```typescript
// Props: { open: boolean; onClose: () => void; onConfirm: () => void; children: ReactNode }
import { ConfirmDialog } from '@workday-core/components/ConfirmDialog';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire StudyMoneyAllocationDialog to real API + add delete confirmation</name>
  <files>
    workday-application/src/main/react/features/budget/StudyMoneyAllocationDialog.tsx,
    workday-application/src/main/react/features/budget/BudgetAllocationFeature.tsx
  </files>
  <action>
    1. Refactor `StudyMoneyAllocationDialog.tsx`:
       - Remove all mock type imports
       - Import wirespec types: `StudyMoneyAllocationInput`, `BudgetAllocationFile` from `../../wirespec/model`
       - Import `BudgetAllocationClient` from `../../clients/BudgetAllocationClient`
       - Props: `{ open: boolean; onClose: () => void; onSaved: () => void; personId?: string }` (personId passed from feature for admin, undefined for non-admin -- backend auto-scopes)
       - On form submit: build `StudyMoneyAllocationInput` from form state, call `BudgetAllocationClient.createStudyMoney(input)`, then call `onSaved()` callback and close dialog
       - For file upload: when user adds files, call `BudgetAllocationClient.uploadFile(file)` for each file, collect returned `{ id, name }` objects, map to `BudgetAllocationFile[]` format (`{ name: result.name, file: result.id }`)
       - Form fields: date (required), description (optional), amount (required, number), files (optional)
       - Show loading state during API call, handle errors with console.error (per Claude's discretion on error handling)

    2. Update `BudgetAllocationFeature.tsx` (from Plan 02) to integrate CRUD:
       - Import `ConfirmDialog` from `@workday-core/components/ConfirmDialog` (check exact import path -- may be `@workday-core` alias)
       - Import `StudyMoneyAllocationDialog`
       - Add state: `dialogOpen` (boolean for create dialog), `deleteTarget` (BudgetAllocation | null for delete confirmation)
       - Wire "Add Study Money" button onClick to `setDialogOpen(true)` (only show button when admin/hasWritePermission)
       - Render `<StudyMoneyAllocationDialog open={dialogOpen} onClose={() => setDialogOpen(false)} onSaved={refresh} personId={isAdmin ? selectedPersonId : undefined} />`
       - Render `<ConfirmDialog open={!!deleteTarget} onClose={() => setDeleteTarget(null)} onConfirm={handleDeleteConfirm}>Are you sure you want to delete this study money allocation?</ConfirmDialog>`
       - `handleDeleteConfirm`: call `BudgetAllocationClient.deleteById(deleteTarget.id)`, then `setDeleteTarget(null)`, then `refresh()`
       - Pass `onDelete={(allocation) => setDeleteTarget(allocation)}` to BudgetAllocationList
       - Pass `onCreate={() => setDialogOpen(true)}` to BudgetAllocationList if it has a create button

    NOTE: Plan 02 creates BudgetAllocationFeature.tsx first. This task modifies it to add CRUD integration. If Plan 02 and 03 execute in parallel, the executor should read the Plan 02 version of BudgetAllocationFeature.tsx before modifying. If sequential, just edit the existing file.
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>StudyMoneyAllocationDialog calls BudgetAllocationClient.createStudyMoney on submit. Delete confirmation uses ConfirmDialog from @workday-core. Both create and delete trigger data refresh. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete mock files, demo component, and prototype documentation</name>
  <files>
    workday-application/src/main/react/features/budget/mocks/BudgetAllocationTypes.ts,
    workday-application/src/main/react/features/budget/mocks/BudgetAllocationMocks.ts,
    workday-application/src/main/react/features/budget/BudgetAllocationDemo.tsx,
    workday-application/src/main/react/features/budget/CHANGES.md,
    workday-application/src/main/react/features/budget/IMPLEMENTATION_SUMMARY.md,
    workday-application/src/main/react/features/budget/PHASE_1_3.md,
    workday-application/src/main/react/features/budget/PHASE_1_3_REVISED.md,
    workday-application/src/main/react/features/budget/PHASE_1_3_SUMMARY.md,
    workday-application/src/main/react/features/budget/README.md
  </files>
  <action>
    1. Delete the entire mocks/ directory:
       - `rm -rf workday-application/src/main/react/features/budget/mocks/`
    2. Delete BudgetAllocationDemo.tsx:
       - `rm workday-application/src/main/react/features/budget/BudgetAllocationDemo.tsx`
    3. Delete Phase 1 prototype documentation files (they served their purpose):
       - `rm workday-application/src/main/react/features/budget/CHANGES.md`
       - `rm workday-application/src/main/react/features/budget/IMPLEMENTATION_SUMMARY.md`
       - `rm workday-application/src/main/react/features/budget/PHASE_1_3.md`
       - `rm workday-application/src/main/react/features/budget/PHASE_1_3_REVISED.md`
       - `rm workday-application/src/main/react/features/budget/PHASE_1_3_SUMMARY.md`
       - `rm workday-application/src/main/react/features/budget/README.md`
    4. Verify no remaining imports reference deleted files:
       - `grep -r "mocks/" workday-application/src/main/react/features/budget/` should return nothing
       - `grep -r "BudgetAllocationDemo" workday-application/src/main/react/` should return nothing (AuthenticatedApplication was updated in Plan 02)
    5. Verify TypeScript still compiles cleanly after deletions.
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>All mock files deleted. BudgetAllocationDemo.tsx deleted. No dangling imports to deleted files. Prototype documentation cleaned up. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project tsconfig.json` -- zero TypeScript errors
- `grep -r "mocks/" workday-application/src/main/react/features/budget/` returns nothing
- `grep -r "BudgetAllocationDemo" workday-application/src/main/react/` returns nothing
- `ls workday-application/src/main/react/features/budget/mocks/` returns "No such file or directory"
- `ls workday-application/src/main/react/features/budget/BudgetAllocationDemo.tsx` returns "No such file or directory"
</verification>

<success_criteria>
- Admin can create StudyMoney allocation via dialog, data persists to database
- Admin can delete StudyMoney allocation with confirmation dialog
- After create/delete, summary cards and allocation list refresh automatically
- All mock types, mock data, and demo component are deleted
- Zero dangling imports to deleted files
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-budget-tab-integration/06-03-SUMMARY.md`
</output>
