---
phase: 09-verification-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workday-application/src/main/react/clients/BudgetAllocationClient.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-domain-layer/03-02-SUMMARY.md
  - .planning/phases/04-persistence-contract/04-VERIFICATION.md
  - .planning/phases/05-api-layer/05-VERIFICATION.md
  - .planning/phases/07-event-integration/07-VERIFICATION.md
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - DOM-04
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - CTR-02
  - EVT-01
  - EVT-02
  - EVT-03
  - EVT-04
gap_closure: true

must_haves:
  truths:
    - "Phase 04 has VERIFICATION.md with passed status for DOM-03 and DOM-04"
    - "Phase 05 has VERIFICATION.md with passed status for API-01 through API-05 and CTR-02"
    - "Phase 07 has VERIFICATION.md with passed status for EVT-01 through EVT-04"
    - "DOM-01 and DOM-02 listed in Phase 3 SUMMARY frontmatter requirements_completed"
    - "DOM-03 checkbox in REQUIREMENTS.md is checked [x]"
    - "BudgetAllocationClient.downloadFile() accepts fileId and fileName, URL matches backend two-segment pattern"
  artifacts:
    - path: ".planning/phases/04-persistence-contract/04-VERIFICATION.md"
      provides: "Formal verification of DOM-03, DOM-04"
      contains: "status: passed"
    - path: ".planning/phases/05-api-layer/05-VERIFICATION.md"
      provides: "Formal verification of API-01 through API-05, CTR-02"
      contains: "status: passed"
    - path: ".planning/phases/07-event-integration/07-VERIFICATION.md"
      provides: "Formal verification of EVT-01 through EVT-04"
      contains: "status: passed"
    - path: "workday-application/src/main/react/clients/BudgetAllocationClient.ts"
      provides: "Fixed downloadFile with two-segment URL"
      contains: "files/${fileId}/${fileName}"
  key_links:
    - from: "BudgetAllocationClient.ts downloadFile"
      to: "BudgetAllocationController @GetMapping files/{file}/{name}"
      via: "URL path pattern match"
      pattern: "files/\\$\\{fileId\\}/\\$\\{fileName\\}"
---

<objective>
Close all verification gaps identified by the v1.0 milestone audit: create missing VERIFICATION.md files for phases 04, 05, and 07; fix documentation gaps in REQUIREMENTS.md and Phase 3 SUMMARY frontmatter; fix latent downloadFile URL bug in BudgetAllocationClient.ts.

Purpose: Ensure all 23 v1 requirements have formal verification artifacts and traceability, and fix the one latent code bug found during audit.
Output: 3 VERIFICATION.md files, 2 documentation fixes, 1 code bugfix, re-audit confirmation of 23/23 requirements satisfied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-verification-gap-closure/09-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-contract-form-dev-data/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix downloadFile bug and documentation gaps</name>
  <files>
    workday-application/src/main/react/clients/BudgetAllocationClient.ts,
    .planning/REQUIREMENTS.md,
    .planning/phases/03-domain-layer/03-02-SUMMARY.md
  </files>
  <action>
Three changes:

1. **Fix downloadFile URL in BudgetAllocationClient.ts** (line 132-133):
   Change the function signature and URL to match the backend controller's two-segment path pattern.

   Current (broken):
   ```typescript
   const downloadFile = (fileId: string): string => {
     return `/api/budget-allocations/files/${fileId}`;
   };
   ```

   Fixed (matches backend `@GetMapping("/api/budget-allocations/files/{file}/{name}")`):
   ```typescript
   const downloadFile = (fileId: string, fileName: string): string => {
     return `/api/budget-allocations/files/${fileId}/${fileName}`;
   };
   ```

   Before changing, grep for any callers of downloadFile to verify it is currently orphaned (exported but never called). The research confirms this, but verify with: `grep -rn "downloadFile" workday-application/src/main/react/` -- should only show the definition and export in BudgetAllocationClient.ts.

2. **Fix DOM-03 checkbox in REQUIREMENTS.md** (line 12):
   Change `- [ ] **DOM-03**:` to `- [x] **DOM-03**:` -- Liquibase migrations were completed in Phase 4 Plan 01.

3. **Add requirements_completed to 03-02-SUMMARY.md frontmatter**:
   Add `requirements_completed: [DOM-01, DOM-02]` to the YAML frontmatter of `.planning/phases/03-domain-layer/03-02-SUMMARY.md`. Place it after the existing frontmatter fields (after `metrics:`). DOM-01 and DOM-02 span both Phase 3 plans, but Plan 02 completed the domain layer (services + tests), so it gets the requirements_completed field.
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday && grep -n 'fileName' workday-application/src/main/react/clients/BudgetAllocationClient.ts | grep -q 'downloadFile' && grep -n '\[x\] \*\*DOM-03\*\*' .planning/REQUIREMENTS.md | grep -q 'DOM-03' && grep -n 'requirements_completed' .planning/phases/03-domain-layer/03-02-SUMMARY.md | grep -q 'DOM-01' && echo "PASS: All 3 fixes verified" || echo "FAIL: One or more fixes missing"</automated>
  </verify>
  <done>
    - downloadFile function accepts (fileId: string, fileName: string) and returns URL with both segments
    - DOM-03 checkbox is [x] in REQUIREMENTS.md
    - 03-02-SUMMARY.md frontmatter includes requirements_completed: [DOM-01, DOM-02]
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VERIFICATION.md for phases 04, 05, 07 and re-audit</name>
  <files>
    .planning/phases/04-persistence-contract/04-VERIFICATION.md,
    .planning/phases/05-api-layer/05-VERIFICATION.md,
    .planning/phases/07-event-integration/07-VERIFICATION.md
  </files>
  <action>
Create 3 VERIFICATION.md files following the exact format from 08-VERIFICATION.md. Each file MUST cite concrete evidence (file paths, line numbers, specific code patterns) -- do NOT use generic evidence like "code exists".

**Evidence gathering approach:** For each phase, read the relevant source files and SUMMARY files to extract concrete line numbers and code patterns. Use grep to find specific evidence.

**04-VERIFICATION.md** -- Verify DOM-03 and DOM-04:

Frontmatter:
```yaml
---
phase: 04-persistence-contract
verified: {current ISO timestamp}
status: passed
score: 10/10 must-haves verified
re_verification: false
---
```

Truths to verify (from 09-RESEARCH.md Gap 1):
- DOM-03 truths (5): Liquibase migrations run clean, JOINED inheritance schema, element collection tables, JPA entities compile, schema test validates
- DOM-04 truths (5): ContractInternal has studyHours/studyMoney, defaults to 0, ContractInternalForm includes fields, migration adds columns, integration test passes

Key artifacts to check:
- `workday-application/src/main/database/db.changelog-027-budget-allocations.yaml`
- `workday-application/src/main/kotlin/.../budget/BudgetAllocationEntity.kt` and child entities
- `workday-application/src/main/database/db.changelog-028-contract-internal-study-budget.yaml`
- `workday-application/src/main/kotlin/.../contract/ContractInternal.kt`
- Test files: `BudgetAllocationSchemaTest.kt`, `ContractInternalPersistenceTest.kt`

Requirements coverage table: DOM-03 (Phase 4 Plan 01), DOM-04 (Phase 4 Plan 03)

**05-VERIFICATION.md** -- Verify API-01 through API-05, CTR-02:

Frontmatter:
```yaml
---
phase: 05-api-layer
verified: {current ISO timestamp}
status: passed
score: 14/14 must-haves verified
re_verification: false
---
```

Truths to verify (from 09-RESEARCH.md Gap 2):
- Plan 05-01 truths (6): Wirespec defines 8 endpoints, contracts.ws has studyHours/studyMoney, Kotlin handlers generated, TS types generated, Authority enum, Spring Configuration
- Plan 05-02 truths (8): GET by person+year, GET by eventCode, non-admin filtering, POST hack-time, POST study-money, DELETE, 403 on mutations, file endpoints

Key artifacts to check:
- `workday-application/src/main/wirespec/budget-allocations.ws`
- `workday-application/src/main/wirespec/contracts.ws`
- `workday-application/src/main/kotlin/.../budget/BudgetAllocationAuthority.kt`
- `workday-application/src/main/kotlin/.../budget/BudgetAllocationConfiguration.kt`
- `workday-application/src/main/kotlin/.../budget/BudgetAllocationController.kt`
- `workday-application/src/main/kotlin/.../budget/BudgetAllocationApiMapper.kt`
- `workday-application/src/test/kotlin/.../budget/BudgetAllocationControllerTest.kt`

Requirements coverage table: CTR-02 (Plan 05-01), API-01 through API-05 (Plan 05-02)

**07-VERIFICATION.md** -- Verify EVT-01 through EVT-04:

Frontmatter:
```yaml
---
phase: 07-event-integration
verified: {current ISO timestamp}
status: passed
score: 5/5 must-haves verified
re_verification: false
---
```

Truths to verify (from 09-RESEARCH.md Gap 3):
- EVT-01 (5 truths): Event dialog shows budget section, per-participant time allocations persist, money allocations persist, FLOCK_HACK_DAY defaults to HackTime, form changes update budget sections

Key artifacts to check:
- `workday-application/src/main/react/clients/BudgetAllocationClient.ts`
- `workday-application/src/main/react/features/event/eventBudgetTransformers.ts`
- `workday-application/src/main/react/features/event/EventDialog.tsx`
- `workday-application/src/main/react/features/event/EventBudgetManagementDialog.tsx`

Requirements coverage table: EVT-01 through EVT-04 (Plan 07-01)

**Re-audit step (final):**
After creating all 3 VERIFICATION.md files, verify:
1. Count all VERIFICATION.md files across all phases -- should be 6 total (03, 04, 05, 06, 07, 08)
2. Count all [x] checkboxes in REQUIREMENTS.md v1 section -- should be 23
3. Grep for requirements_completed across all SUMMARY files to confirm DOM-01, DOM-02 now tracked
4. Confirm downloadFile URL fix is in place
Output a brief re-audit summary confirming 23/23 requirements satisfied.
  </action>
  <verify>
    <automated>cd /Users/julius.van.dis/IdeaProjects/Flock/flock-eco-workday && test -f .planning/phases/04-persistence-contract/04-VERIFICATION.md && test -f .planning/phases/05-api-layer/05-VERIFICATION.md && test -f .planning/phases/07-event-integration/07-VERIFICATION.md && grep -c "status: passed" .planning/phases/04-persistence-contract/04-VERIFICATION.md .planning/phases/05-api-layer/05-VERIFICATION.md .planning/phases/07-event-integration/07-VERIFICATION.md | grep -c ":1$" | grep -q "3" && echo "PASS: All 3 VERIFICATION.md files exist with passed status" || echo "FAIL: Missing or invalid VERIFICATION.md files"</automated>
  </verify>
  <done>
    - 04-VERIFICATION.md exists with passed status, cites concrete evidence for DOM-03 and DOM-04
    - 05-VERIFICATION.md exists with passed status, cites concrete evidence for API-01 through API-05 and CTR-02
    - 07-VERIFICATION.md exists with passed status, cites concrete evidence for EVT-01 through EVT-04
    - Re-audit confirms 23/23 v1 requirements satisfied with full traceability
  </done>
</task>

</tasks>

<verification>
1. All 6 VERIFICATION.md files exist across phases 03, 04, 05, 06, 07, 08
2. REQUIREMENTS.md shows 23/23 checkboxes marked [x] in v1 section
3. downloadFile in BudgetAllocationClient.ts uses two-segment URL pattern matching backend
4. 03-02-SUMMARY.md frontmatter includes requirements_completed with DOM-01, DOM-02
</verification>

<success_criteria>
- All 14 phase requirements (DOM-01, DOM-02, DOM-03, DOM-04, API-01-05, CTR-02, EVT-01-04) have formal verification artifacts
- downloadFile URL pattern matches backend controller path (`/api/budget-allocations/files/{file}/{name}`)
- Re-audit shows 23/23 requirements satisfied with zero gaps
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-gap-closure/09-01-SUMMARY.md`
</output>
