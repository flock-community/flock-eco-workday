databaseChangeLog:
  - changeSet:
      id: db.changelog-027-user-kratos-identity-id
      author: roman
      changes:
        - addColumn:
            tableName: user
            columns:
              - column:
                  name: kratos_identity_id
                  type: varchar(255)
                  constraints:
                    nullable: true
                    unique: true
                    uniqueConstraintName: UC_USER_KRATOS_IDENTITY_ID
